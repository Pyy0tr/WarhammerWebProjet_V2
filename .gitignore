# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
*.egg

# Data — generated files
data/raw/
data/cache/
data/users.db

# V11 staging pipeline — artefacts régénérables, pas commités
# (data/version_v11.json reste tracké : c'est l'état persistant utilisé par
# fetch_bsdata_v11.py pour détecter les mises à jour d'un run CI à l'autre,
# même rôle que data/version.json pour la V10)
data/raw_v11/
data/cache_v11/
data/cache_stable_v11/
data/frontend_preview_v11/

# Environment
.env
.env.local
backend/.env

# Node / Frontend
node_modules/
dist/
.next/
.nuxt/
build/

# Logs
*.log
logs/

# Backup files
*.backup.json
# Terraform
infra/.terraform/
infra/terraform.tfstate
infra/terraform.tfstate.backup
infra/terraform.tfvars
infra/.terraform.lock.hcl

# AWS CLI installer (artefacts d'installation, pas du code)
aws/
awscliv2.zip

# Fichiers créés accidentellement
apt
sudo
update

# Vite env — contenu injecté par CI/CD via secrets GitHub Actions
frontend/.env.production
frontend/.env.local

# Archives et binaires d'installation
*.zip
*.tar.gz

# OS
.DS_Store
Thumbs.db

# IDE — préférences personnelles, pas du code
.vscode/
.idea/
*.swp
*.swo

# ML — modèle trop lourd pour git (257 MB), dataset brut régénérable
data/ml/model/
data/ml/dataset_raw.jsonl
data/ml/venv/

# Interne — ne pas exposer publiquement
bugsRequest/
CLAUDE.md
*_NOTES.txt
